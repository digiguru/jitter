{
  "name": "jitter-audio-visualizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "lint": "eslint . && html-validate index.html",
    "test": "vitest run tests/audioControl.test.js",
    "test:watch": "vitest tests/audioControl.test.js",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "eslint": "^10.9.0",
    "html-validate": "^11.9.0",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.11"
  }
}
